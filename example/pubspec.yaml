name: cupertino_fundations_models_example
description: Manual example app for cupertino_fundations_models.
publish_to: none
version: 0.0.1

environment:
  sdk: ^3.11.0

dependencies:
  cupertino_fundations_models:
    path: ..
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
